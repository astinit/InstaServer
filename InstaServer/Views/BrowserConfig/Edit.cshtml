@using InstaServer.Models;
@model BrowserConfig
@{
    ViewData["Title"] = "BrowserConfigs";
}
<section class="col-md-6">
    <h3>BrowserConfigs</h3>
    <h5 class="text-danger">@TempData["Error"]</h5>
    <h5 class="text-success">@TempData["Success"]</h5>
    <form asp-controller="BrowserConfig" asp-action="Edit" method="post">
        <div class="form-group">
            <label for="SessionId">SessionId</label>
            <input id="SessionId" asp-for="SessionId" type="text" value="@Model.SessionId" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
</section>